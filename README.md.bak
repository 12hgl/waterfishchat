# 水鱼 Chat (Waterfish Chat)

轻量级多提供商 AI 聊天网页，Docker 一键部署，支持自动空闲休眠。

## 特性

- 多 AI 提供商支持（OpenAI 兼容 API）
- CF Turnstile 登录保护
- 自动空闲休眠（默认 30 分钟无访问自动停止容器）
- 轻量镜像（< 80MB）
- 数据持久化（SQLite 存储配置与对话）

## 快速部署

```bash
docker compose up -d
```

访问 `http://localhost:8080`，完成初始化向导后即可使用。

## 手动拉取镜像

```bash
docker pull ghcr.io/12hgl/waterfishchat:latest
docker pull ghcr.io/12hgl/waterfishchat:v1.0
```

## 配置

| 环境变量 | 默认值 | 说明 |
|---------|--------|------|
| `IDLE_TIMEOUT_MIN` | 30 | 空闲休眠时间（分钟） |
| `TURNSTILE_SITE_KEY` | - | Cloudflare Turnstile 站点密钥 |
| `TURNSTILE_SECRET_KEY` | - | Cloudflare Turnstile 密钥 |

## 仓库

https://github.com/12hgl/waterfishchat
